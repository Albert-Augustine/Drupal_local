<?php


  function custom_module_form_alter(&$form, &$form_state, $form_id) {
    if ($form_id == 'node_district_form') {
      $form['name'] = array(
        '#type' => 'textfield',
        '#title' => t('Candidate Name:'),
        '#required' => TRUE,
      );
    }
  }
  